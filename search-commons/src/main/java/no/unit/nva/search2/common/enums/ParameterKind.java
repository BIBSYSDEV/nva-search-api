package no.unit.nva.search2.common.enums;

public enum ParameterKind {
    INVALID,
    IGNORED,
    BOOLEAN,
    NUMBER,
    DATE,
    KEYWORD,
    FUZZY_KEYWORD,
    TEXT,
    FUZZY_TEXT,
    FREE_TEXT,
    SORT_KEY,
    CUSTOM
}
