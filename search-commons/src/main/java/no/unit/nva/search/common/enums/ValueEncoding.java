package no.unit.nva.search.common.enums;

/**
 * Enum for defining the encoding of values used in the search service.
 *
 * @author Stig Norland
 */
public enum ValueEncoding {
    NONE,
    DECODE
}
